// Exercise 11 from Chapter 1 of C programming
// File 1ex11.c

// This C blog post will describe how I would test the word 
// count program. It will also describe what input is most
// likely to uncover bugs if any.

#include <stdio.h>

//#define IN  1 /* inside a word */
//#define OUT 0 /* outside a word */

/* count lines, words, and characters in input */

main()
{

	printf("I would test the program by compiling it.\n");
	printf("But I sure as hell won't be one of the many programmers\n");
	printf("who use the tired phrase: \"Hello World\" \n");

	/*

	int c, nl, nw, nc, state;

	state = OUT;
	nl = nw = nc = 0; // initialize these variables ie start these variables with the value of zero

	while ((c = getchar()) != EOF) {
		++nc; // Upon encountering the first character of a word in line 18, a new word count occurs
		if (c == ' ' || c == '\n' || c == '\t')
			state = OUT; // Line 20 says if c is equal to blanks, newlines, or tabs, understand that these characters are not part of words
		else if (state == OUT) {
			state = IN;
			*/
		}
	}
}